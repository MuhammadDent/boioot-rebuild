import StaticPageView from "@/components/pages/StaticPageView";

export const metadata = { title: "سياسة الاستخدام — بيوت" };

export default function UsagePolicyPage() {
  return <StaticPageView slug="usage-policy" />;
}
