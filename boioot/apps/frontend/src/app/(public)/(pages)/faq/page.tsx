import StaticPageView from "@/components/pages/StaticPageView";

export const metadata = { title: "الأسئلة الشائعة — بيوت" };

export default function FaqPage() {
  return <StaticPageView slug="faq" />;
}
