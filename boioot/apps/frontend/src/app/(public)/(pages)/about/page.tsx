import StaticPageView from "@/components/pages/StaticPageView";

export const metadata = { title: "من نحن — بيوت" };

export default function AboutPage() {
  return <StaticPageView slug="about" />;
}
