import StaticPageView from "@/components/pages/StaticPageView";

export const metadata = { title: "الشروط والأحكام — بيوت" };

export default function TermsPage() {
  return <StaticPageView slug="terms" />;
}
