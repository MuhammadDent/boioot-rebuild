import StaticPageView from "@/components/pages/StaticPageView";

export const metadata = { title: "تواصل معنا — بيوت" };

export default function ContactPage() {
  return <StaticPageView slug="contact" />;
}
