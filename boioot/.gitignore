# OS
.DS_Store
Thumbs.db

# Node
node_modules/
.next/
out/
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.*

# .NET
bin/
obj/
*.user
*.suo
*.cache
*.pdb

# Replit
.replit
.replit.nix

# Misc
# NOTE: "coverage/" intentionally removed — it was incorrectly ignoring
# Next.js app routes (src/app/dashboard/coverage) and feature modules
# (src/features/coverage). Each package's own .gitignore handles
# test coverage report directories.
