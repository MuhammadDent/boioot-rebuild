You are a Senior Next.js + Image Optimization Engineer.

We have a real estate platform where the cover image is blurry and oversized.

This is NOT just a CSS issue — fix it properly.

-------------------------------------
🎯 ROOT PROBLEM ANALYSIS (IMPORTANT)
-------------------------------------

The blur is caused by one or more of:

1) Low resolution image being stretched
2) Wrong image variant (thumbnail instead of original)
3) Improper container sizing
4) Using <img> instead of optimized rendering

You MUST verify all of these.

-------------------------------------
🎯 TASK 1: Fix Cover Image Rendering (CRITICAL)
-------------------------------------

In property details page:

1. Replace ANY <img> with Next.js <Image>

2. Use:

<Image
  src={coverImageUrl}
  alt="cover"
  fill
  priority
  sizes="(max-width: 768px) 100vw, 1200px"
  style={{ objectFit: "cover" }}
/>

3. Wrap it in container:

<div className="cover-container">

CSS:

.cover-container {
  position: relative;
  width: 100%;
  height: 380px;
  max-height: 60vh;
  overflow: hidden;
  border-radius: 16px;
}

@media (max-width: 768px) {
  .cover-container {
    height: 240px;
  }
}

-------------------------------------
🎯 TASK 2: Detect Image Source Problem
-------------------------------------

Check the URL of the cover image:

- If it includes:
  thumb / small / preview
→ THIS IS WRONG

We must use ORIGINAL image from R2

Fix logic so:
- cover image uses FULL resolution image
- thumbnails use smaller images (if needed)

-------------------------------------
🎯 TASK 3: Prevent Upscaling (IMPORTANT)
-------------------------------------

If image width < 1000px:

- DO NOT stretch it to full width
- Instead:
  center it with background

Optional enhancement:

.cover-container img {
  object-fit: contain;
  background: #f5f5f5;
}

OR better:
- detect low-res images and apply contain mode

-------------------------------------
🎯 TASK 4: Improve Thumbnail → Cover Logic
-------------------------------------

When user clicks thumbnail:

- Ensure selected image is high quality
- Do NOT reuse low-res preview version

-------------------------------------
🎯 TASK 5: Add Debug Log (TEMPORARY)
-------------------------------------

Log in console:

console.log("Cover Image URL:", coverImageUrl);

So we can confirm:
- Are we using correct R2 file?
- Or a compressed version?

-------------------------------------
🎯 TASK 6: Future-proof (IMPORTANT)
-------------------------------------

Prepare system for:

- original image
- medium image
- thumbnail

Even if not implemented now

-------------------------------------
🎯 RULES
-------------------------------------

- Do NOT break current functionality
- Do NOT change backend API
- Only fix rendering + image selection
- Keep code clean and reusable

-------------------------------------
🎯 OUTPUT REQUIRED
-------------------------------------

After implementation explain:

1) Was the issue CSS or image source?
2) What was wrong exactly?
3) What file was modified?
4) What is now used: original or thumbnail?