I want to CONNECT my existing analytics dashboard to Google Analytics 4 (GA4),
verify all tracking events, and enable real data monitoring in a SAFE and PROFESSIONAL way.

IMPORTANT RULES:
- Do NOT break existing tracking system
- Do NOT modify business logic
- Do NOT block UI if GA fails
- Do NOT expose GA secrets to frontend
- Keep everything production-safe and lightweight
- Use best practices for GA4 integration

GOAL:
Turn the current /dashboard/insights page from mock data → REAL DATA using GA4

---

PHASE 1: Verify GA4 tracking (frontend)

1. Ensure gtag is properly initialized globally
2. Confirm trackEvent() sends events correctly to GA4
3. Add console debug logs ONLY in development mode

Add a temporary debug helper:
- log all events fired
- confirm event_name and props

DO NOT affect production performance

---

PHASE 2: Add GA4 Debug Validation Mode

Add optional debug flag:

?debugAnalytics=true

When enabled:
- show small debug panel (bottom corner)
- display last 10 events fired
- display event payload
- only in development mode

---

PHASE 3: Create backend GA4 Reporting API proxy

IMPORTANT:
Frontend must NOT call GA directly.

Create secure backend endpoint:

GET /api/analytics/summary

This endpoint will:
- call GA4 Data API (Google Analytics Reporting API)
- use server-side credentials (env variables)
- return aggregated data

Use:

Metrics:
- eventCount

Dimensions:
- eventName

Events to query:
- view_property
- booking_start
- booking_submit
- click_whatsapp
- upgrade_prompt_viewed
- upgrade_clicked
- listing_created

Also compute:
- conversion rates (server-side)

---

PHASE 4: Environment setup

Add environment variables:

GA4_PROPERTY_ID=
GA4_CLIENT_EMAIL=
GA4_PRIVATE_KEY=

Use Google service account authentication

DO NOT expose these values to frontend

---

PHASE 5: Connect dashboard to real data

Update:

/dashboard/insights/page.tsx

Replace mock data with fetch:

/api/analytics/summary

Handle:
- loading state
- empty state
- error fallback (show "No data yet")

DO NOT crash UI

---

PHASE 6: Data mapping

Map GA events → dashboard sections:

Overview:
- view_property → views
- booking_submit → bookings
- click_whatsapp → contacts
- listing_created → listings

Funnel:
- view_property
- click_whatsapp
- booking_start
- booking_submit

Top Performers:
(leave placeholder or TODO if GA dimension not ready)

Monetization:
- upgrade_prompt_viewed
- upgrade_clicked
- conversion rate

---

PHASE 7: Validation

Verify:

1. Events appear in GA DebugView
2. Dashboard shows real data
3. If GA fails → dashboard still loads
4. No sensitive data is sent
5. No console errors in production
6. Performance not affected

---

PHASE 8: Developer Notes

Add clear comments:

- where GA is configured
- where to extend events
- where to add future analytics

---

FINAL OUTPUT:

Return:

1. Files created/modified
2. Backend endpoint implementation
3. How GA credentials are used securely
4. How to test events in GA DebugView
5. Confirmation that system is non-blocking and safe

---

GOAL:
A production-ready analytics pipeline:
Tracking → GA4 → Backend → Dashboard

No overengineering, clean, scalable, and monetization-ready.