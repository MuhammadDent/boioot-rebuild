We are upgrading the Admin Dashboard to a TRUE SaaS Business Intelligence system.

IMPORTANT:
- Do NOT change backend APIs
- Do NOT modify database
- Use existing data only
- Focus on frontend logic + computed insights

==================================================
GOAL
==================================================

Transform dashboard from:
"Metrics display"

Into:
"Business decision engine"

==================================================
TASK 1 — DRILL DOWN SYSTEM
==================================================

Make ALL KPI cards clickable.

When clicked:

- Navigate to filtered page
- OR open modal with details

Examples:

- Users KPI → opens users page filtered by time range
- Listings KPI → opens listings with status filters
- Requests KPI → opens pending requests

==================================================
TASK 2 — SEGMENTATION (CRITICAL)
==================================================

Break KPIs into segments:

Users:
- مالك
- وسيط
- شركة

Subscriptions:
- مجاني
- مدفوع

Listings:
- بيع
- إيجار
- يومي

Display as:
- Tabs OR mini breakdown inside card

==================================================
TASK 3 — REAL SAAS METRICS
==================================================

Add:

1) MRR (Monthly Recurring Revenue)
2) ARPU (Revenue / active users)
3) Conversion Rate (Free → Paid)
4) Churn Rate (users leaving / inactive)

If exact data not available:
- Estimate from existing invoices + plans

==================================================
TASK 4 — FUNNEL VIEW
==================================================

Add simple funnel:

- تسجيل مستخدم
→ إنشاء إعلان
→ طلب اشتراك
→ دفع

Show drop-off between each stage

==================================================
TASK 5 — SMART INSIGHTS (AI-LIKE)
==================================================

Add "Insights Box":

Examples:

- "60% من المستخدمين لا يكملون نشر إعلان"
- "الإعلانات اليومية تحقق تفاعل أعلى"
- "الوسطاء لديهم أعلى معدل تحويل"

This can be computed heuristically (no AI needed)

==================================================
TASK 6 — PRIORITY ACTION PANEL
==================================================

Add section:

"ما يجب عليك فعله الآن"

Examples:

- مراجعة 12 طلب اشتراك
- تفعيل 8 إعلانات متوقفة
- متابعة 5 مستخدمين جدد

==================================================
TASK 7 — EXPORT & REPORTS
==================================================

Add:

- Export CSV button
- Print report

==================================================
FINAL RESULT
==================================================

Admin dashboard becomes:

- Business brain
- Decision-making tool
- Growth engine

==================================================
DO NOT:
==================================================

- Do NOT modify backend
- Do NOT break UI