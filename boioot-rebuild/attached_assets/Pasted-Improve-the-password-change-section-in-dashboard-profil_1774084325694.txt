Improve the password change section in /dashboard/profile to make it production-ready.

==================================================
PROBLEMS TO FIX
==================================================

1. There is no show/hide password toggle for:
   - current password
   - new password
   - confirm password

2. There is no visible success message when password change succeeds.

3. There is no visible error message when password change fails.

4. UX is incomplete and does not feel like a professional SaaS settings page.

==================================================
GOAL
==================================================

Upgrade the Security / Change Password form with proper UX and feedback.

==================================================
REQUIREMENTS
==================================================

1. Add show/hide toggle buttons (eye icon) for:
   - Current Password
   - New Password
   - Confirm Password

2. Each password field should support switching between:
   - type="password"
   - type="text"

3. Add clear feedback states:
   - success alert/toast/message when password changes successfully
   - error alert/toast/message when API returns failure
   - validation error when new password and confirm password do not match

4. Add loading state:
   - disable submit button while request is in progress
   - show loading text or spinner

5. Add basic validation:
   - current password required
   - new password required
   - confirm password required
   - new password must match confirm password
   - optionally enforce minimum length

6. Keep RTL layout and Arabic UI fully supported.

==================================================
UX DETAILS
==================================================

- Place the eye toggle inside each input field
- Use clear Arabic labels
- Show inline validation messages under fields
- Show success message in green
- Show error message in red
- Keep the design consistent with a premium SaaS dashboard

==================================================
EXAMPLE ARABIC TEXTS
==================================================

Labels:
- كلمة المرور الحالية
- كلمة المرور الجديدة
- تأكيد كلمة المرور الجديدة

Validation:
- هذا الحقل مطلوب
- كلمة المرور الجديدة وتأكيدها غير متطابقين
- يجب أن تكون كلمة المرور 8 أحرف على الأقل

Success:
- تم تغيير كلمة المرور بنجاح

Error:
- فشل تغيير كلمة المرور
- كلمة المرور الحالية غير صحيحة
- حدث خطأ غير متوقع، حاول مرة أخرى

Button states:
- حفظ التغييرات
- جاري الحفظ...

==================================================
TECHNICAL NOTES
==================================================

- Do not break the existing API integration
- Only enhance the form UX, validation, and feedback
- Reuse existing component patterns if available
- Keep the code modular and clean

==================================================
OUTPUT
==================================================

Return the updated password form component and any helper logic added for:
- password visibility toggles
- validation
- success/error state handling