We need to improve the location filters on the properties page.

Current issue:
- The district/neighborhood field is not implemented as the proper dropdown behavior we need
- We also need province/governorate support in the filters

==================================================
GOAL
==================================================

Upgrade the location filtering system so it supports:

1. Governorate / province dropdown
2. City dropdown
3. District / neighborhood dropdown

And make the neighborhood dropdown dependent on the selected city/governorate when possible.

==================================================
REQUIRED FILTER STRUCTURE
==================================================

Replace the current location filtering approach with the following structure:

- المحافظة / المدينة (dropdown)
- الحي / المنطقة (dropdown)

If your current data model supports both governorate and city separately, use:

- المحافظة (dropdown)
- المدينة (dropdown)
- الحي / المنطقة (dropdown)

If not, at minimum implement:
- المدينة / المحافظة (dropdown)
- الحي / المنطقة (dropdown)

==================================================
BEHAVIOR REQUIREMENTS
==================================================

1. The city/governorate filter must be a dropdown, not hardcoded incorrectly
2. The district/neighborhood filter must also be a dropdown, not a free text field
3. The neighborhood options should be generated dynamically from the full original properties dataset
4. The neighborhood dropdown should update based on the selected city/governorate
5. Remove duplicates from all dropdown options
6. Sort options in a clean readable order
7. Keep "all" option at the top

==================================================
DATA SOURCE RULE
==================================================

Do NOT hardcode the options manually unless you are using a dedicated lookup/master data source.

Build the options from:
- the full raw properties dataset before filtering
- not from only currently visible properties
- not from already filtered subset

==================================================
FILTERING LOGIC
==================================================

Make sure filtering works correctly:

- selecting city/governorate filters the results
- selecting neighborhood filters within that location
- if city changes, neighborhood options refresh
- if selected neighborhood becomes invalid after city change, reset it safely

==================================================
UI REQUIREMENTS
==================================================

- Use select dropdowns for both fields
- Keep RTL layout
- Keep the current filter panel style
- Do not redesign the whole page
- Only improve the location filters properly

==================================================
IMPORTANT DEBUG CHECK
==================================================

Please inspect the property data model and confirm:
- what field represents governorate or city
- what field represents district / neighborhood
- whether some values are mixed incorrectly

Normalize the data if needed before generating filter options.

==================================================
EXPECTED RESULT
==================================================

The filter panel should allow the user to select:
- محافظة / مدينة from dropdown
- حي / منطقة from dropdown

And both should work correctly and dynamically with the property data.

After implementation, report:
1. which field is used for city/governorate
2. which field is used for neighborhood
3. whether neighborhood options are dependent on the selected city