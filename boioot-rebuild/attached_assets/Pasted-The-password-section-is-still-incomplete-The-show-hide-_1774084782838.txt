The password section is still incomplete.

The show/hide password feature was NOT actually implemented.
I still cannot reveal the current password or the new password fields.

Fix this properly in the Security section of /dashboard/profile.

==================================================
REQUIRED FIX
==================================================

Add real password visibility toggles for these fields:
1. current password
2. new password
3. confirm password

Each field must have:
- an eye icon button inside the input
- toggle between type="password" and type="text"
- its own independent visibility state

==================================================
IMPLEMENTATION DETAILS
==================================================

1. Add separate state variables, for example:
- showCurrentPassword
- showNewPassword
- showConfirmPassword

2. Input types must change dynamically:
- password when hidden
- text when visible

3. Add a clickable eye / eye-off icon inside each password input.

4. The toggle button must be positioned correctly for RTL layout.
Since the UI is Arabic, place the toggle icon inside the input in a way that looks correct in RTL.

5. Do not break existing validation, API calls, loading state, or success/error messages.

==================================================
UX REQUIREMENTS
==================================================

- The icon must be visible and clickable
- The input should have enough padding so text does not overlap with the icon
- The icon should reflect current state:
  - eye when hidden
  - eye-off when visible
- Keep accessibility:
  - type="button"
  - aria-label in Arabic such as:
    - إظهار كلمة المرور
    - إخفاء كلمة المرور

==================================================
IMPORTANT
==================================================

Do not just describe the feature.
Actually implement it in the code and make sure it renders in the UI.

Return the updated password field JSX and the state logic you added.