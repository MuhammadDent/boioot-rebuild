We need to improve the business location section in /dashboard/profile.

==================================================
GOAL
==================================================

Replace free-text location structure with a professional hierarchical location system.

Required location structure:

1) Governorate / Province
2) City
3) District / Neighborhood

All 3 fields must be dropdowns.

==================================================
UI / UX REQUIREMENTS
==================================================

1) Add these fields in the business location section:
- Governorate (dropdown)
- City (dependent dropdown)
- District (dependent dropdown)

2) Dropdown behavior:
- City options depend on selected Governorate
- District options depend on selected City

3) If a value is not available:
Provide an option like:
- "Add new governorate"
- "Add new city"
- "Add new district"

4) Clicking "Add new" should open a small modal/dialog:
- user can enter the missing value
- after saving, it becomes selectable immediately

5) Keep Address as a normal text field:
- detailed address remains free text

6) Keep map picker as-is:
- user can still select map location
- latitude / longitude remain required

==================================================
DATA / ARCHITECTURE REQUIREMENTS
==================================================

1) Do NOT keep this only as loose text fields.
Create proper structured location entities if not already موجودة:

- Governorate
- City
- District

2) Relations:
- Governorate -> many Cities
- City -> many Districts

3) Business profile should store selected IDs cleanly:
- governorateId
- cityId
- districtId

4) The UI should load dropdown options from backend/API, not hardcoded arrays.

5) Still allow fallback support for custom additions when a value does not exist.

==================================================
IMPORTANT BUSINESS RULES
==================================================

1) This must work in /dashboard/profile
not onboarding only.

2) Existing saved values must load correctly.

3) RTL support is required.

4) Follow current design system.

5) Reuse the location component if onboarding also uses it.

==================================================
EXPECTED RESULT
==================================================

In the profile page, I want:
- Governorate dropdown
- City dropdown
- District dropdown
- ability to add missing option
- detailed address field
- map picker
- save everything correctly