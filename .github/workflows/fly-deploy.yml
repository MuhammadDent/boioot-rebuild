# This workflow has been DISABLED to avoid duplicate deployments.
# Active workflow: deploy-backend.yml
#
# This file is kept to preserve GitHub Actions run history.
# Do NOT re-enable the push trigger here.

name: Fly Deploy (Disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use deploy-backend.yml instead.'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow is disabled
        run: |
          echo "This workflow is disabled."
          echo "Use 'Deploy Backend to Fly.io' workflow instead."
          exit 0
